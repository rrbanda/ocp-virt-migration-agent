# Environment files with secrets
.env
*.env.local

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Node (if building UI locally)
node_modules/
dist/

# IDE / editor
.idea/
.vscode/
.cursor/
*.swp
*.swo
.DS_Store

# Kubernetes secrets (never commit plaintext)
*-secret.yaml
*-credentials.yaml
!deploy/sealed-secrets/*.yaml

# Helm temp files
.helm-secrets.yaml

# Session handoff docs (contain cluster URLs and credential hints)
HANDOFF.md

# Gitleaks cache and reports
.gitleaks-report.json


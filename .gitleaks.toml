[extend]
useDefault = true

[allowlist]
description = "Safe files: SealedSecrets (encrypted) and env example (placeholders)"
paths = [
    '''deploy/sealed-secrets/.*\.yaml''',
    '''\.env\.example''',
]
